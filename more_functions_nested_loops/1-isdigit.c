#include "main.h"

/**
 * _isupper - check for uppercase character
 * Return: 1 uppercase and 0 otherwise
 * @c: character to be checked
 */

int _isdigit(int c);
{
	if (c >= 48 && c <= 57)
		return (1);
	return (0);
}
