#include "main.h"
/**
 * _strlen - function that returns the length of a string
 * @s: pointer that will update the value of s
 * Return: 0
 */
int _strlen(char *s)
{
	int c = *a;
	*a = *b;
	*b = c;
}
