#include <stdio.h>
#include "main.h"
/**
 * main - program that calls its name
 * @argc - argument count
 * @argv - vector
 * Return: 0
 */


int main (int argc, char *argv[])
{
	int = 1;

	if (argv > 1)
	{
		printf("argv[%d] = %s\n", i, argv[i]);
	}
	return 0;
}

