#include "main.h"
 /**
	* _islower - check lowercase character
	* Description: print alphabet 10 times
	* Return: zero
*/

int _islower(int c)

{

	if (c >= 97 && c <= 122)
		return (1);
	else
		return (0);
}
